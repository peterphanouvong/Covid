export namespace Types {
  export type Data = {
    Specification: string;
    Keyword: string;
    "RGB hex value": string;
  };

  export type PopulationData = {
    Country: string;
    Population: number;
  };
}
